const genericFailure = "We couldn't send your enquiry. Please try again or call 0405878406.";const successMessage = 'Enquiry sent successfully.';const uuidPattern = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;const safeErrors = new Map([  ['400:VALIDATION_ERROR', 'Check the highlighted fields and try again.'],  ['403:ORIGIN_REJECTED', 'This form must be submitted from the Ellis Services Group website.'],  ['413:PHOTO_TOO_LARGE', 'Photo must be 4 MB or smaller.'],  ['415:UNSUPPORTED_PHOTO_TYPE', 'Choose a JPG, PNG or WebP photo.'],  ['429:RATE_LIMITED', 'Too many attempts. Please wait 10 minutes and try again.'],]);const fields = ['name', 'email', 'phone', 'address', 'area', 'service', 'message', 'privacy', 'website'];export class ContactApiError extends Error {  constructor({ status, code, fieldErrors, requestId, message }) {    super(message);    this.name = 'ContactApiError';    this.status = status;    this.code = code;    this.fieldErrors = fieldErrors;    this.requestId = requestId;  }}function genericError() {  return new ContactApiError({ message: genericFailure });}function isObject(value) {  return Boolean(value) && typeof value === 'object' && !Array.isArray(value);}function formControl(form, name) {  return form.elements.namedItem(name);}export function buildContactFormData(form) {
